const menuButton=document.querySelector('.menu-button');const nav=document.querySelector('.nav');menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
document.querySelectorAll('[data-formspree]').forEach(form=>form.addEventListener('submit',async event=>{const status=form.querySelector('.form-status');if(form.action.includes('YOUR_FORM_ID')){event.preventDefault();status.className='form-status error';status.textContent='Preview only: replace YOUR_FORM_ID with Avery’s Formspree form ID before launch.';return}event.preventDefault();const button=form.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Sending…';try{const response=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});if(!response.ok)throw new Error('Submission failed');form.reset();status.className='form-status success';status.textContent='Thanks! Your message has been sent to Avery.';button.textContent='Sent ✓'}catch(error){status.className='form-status error';status.textContent='That did not send. Please try again or contact Avery on Instagram.';button.disabled=false;button.textContent='Send message'}}));
